services:
  db:
    image: postgres:18-alpine3.23
    container_name: db
    restart: unless-stopped
    expose:
      - "5432"
    env_file:
      - .env
    volumes:
      - type: volume
        source: shorty-db
        target: /var/lib/postgresql
    healthcheck:
      test: ["CMD-SHELL", "pg_isready"]
      start_period: 10s
      start_interval: 1s
      interval: 30s
      timeout: 5s
      retries: 3
    networks:
      - backend-net

  migrations:
    build: .
    image: shorty-app:v1.0
    container_name: migrations
    env_file:
      - .env
    environment:
      - POSTGRES_HOST=db
    depends_on:
      db:
        condition: service_healthy
    networks:
      - backend-net
    command: ["alembic", "upgrade", "head"]

  redis:
    image: redis:8.2.4-alpine3.22
    container_name: redis
    restart: unless-stopped
    expose:
      - "6379"
    env_file:
      - .env
    volumes:
      - type: volume
        source: shorty-redis
        target: /data
    healthcheck:
      test: [ "CMD-SHELL", "redis-cli", "ping" ]
      start_period: 10s
      start_interval: 1s
      interval: 30s
      timeout: 5s
      retries: 3
    networks:
      - backend-net

  app:
    image: shorty-app:v1.0
    container_name: app
    restart: unless-stopped
    expose:
      - "8000"
    env_file:
      - .env
    environment:
      - POSTGRES_HOST=db
      - REDIS_HOST=redis
    depends_on:
      migrations:
        condition: service_completed_successfully
      redis:
        condition: service_healthy
    networks:
      - backend-net
      - frontend-net

  nginx:
    build:
      context: frontend
    image: shorty-nginx:v1.0
    container_name: nginx
    restart: unless-stopped
    depends_on:
      app:
        condition: service_started
    ports:
      - target: 80
        published: 80
        protocol: tcp
        mode: host
    networks:
      - frontend-net


networks:
  backend-net:
    name: backend-net
  frontend-net:
    name: frontend-net

volumes:
  shorty-db:
    name: shorty-db
  shorty-redis:
    name: shorty-redis